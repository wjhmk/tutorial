<% if @feed_items.any? %>
  <ol class="microposts">
      <!--@feed_itemsの各要素がMicropostクラスを持ってるため、RailsはMicropostのパーシャル(_micropost.html.erb)を呼び出すことができる。-->
    <%= render @feed_items %>
  </ol>
  <%= will_paginate @feed_items %>
<% end %>